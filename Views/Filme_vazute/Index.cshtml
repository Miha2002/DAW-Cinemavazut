@model IEnumerable<Proiect_DAW_Cinemavazut.Models.Filme_vazute>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.comentariu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.favorit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_film)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_utilizator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.comentariu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.favorit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_film)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_utilizator)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_vazute">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_vazute">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_vazute">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
